<html>
<head>
  <title>Blog Edit</title>
  <link rel="stylesheet" type="text/css" href="/css/bootstrap.min.css" />
  <style>
  body{
    background: url("/image/background.png") no-repeat fixed center;
    color: white;
  }
  .card-body{
    background-color: rgba(0, 0, 0, 0.6);
  }
  </style>
</head>

<body>
<div class="container">
<h1>Edit Article</h1>
<form action="/<%= article._id %>?_method=PUT" method="POST">
<div class="card mt-4 bg-transparent">
    <div class="card-body">
    <div class="form-group">
    <label for="title">Title</label>
    <input required value="<%= article.title %>" type="text" name="title" id="title" class="form-control bg-transparent text-light">
    </div>

    <div class="form-group">
    <label for="description">Description</label>
    <textarea name="description" id="description" class="form-control bg-transparent text-light"><%= article.description %></textarea>
    </div>

    <div class="form-group">
    <label for="content">Content</label>
    <textarea name="content" id="content" class="form-control bg-transparent text-light"><%= article.content %></textarea>
    </div>

    <a href="/" class="btn btn-secondary">Cancel</a>
    <button type="submit" class="btn btn-primary">Save</button>
    </div>
</div>    
</form>
</div>
</body>

</html>